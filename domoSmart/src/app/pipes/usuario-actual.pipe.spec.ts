import { UsuarioActualPipe } from './usuario-actual.pipe';

describe('UsuarioActualPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioActualPipe();
    expect(pipe).toBeTruthy();
  });
});
